# TreeTab

**以看板形式管理你的浏览器标签页**

TreeTab 是一个 Chrome 扩展，将新标签页替换为一个直观的标签管理看板：
- **上方**：Chrome 原生标签组（横向滚动，支持拖拽管理）
- **下方**：按域名分组的标签页列表

无服务器，无账户，无外部 API 调用。纯 Chrome 扩展。

---

## 快速安装

### 方式一：编码 Agent 安装

将本仓库发给你的编码 Agent（Claude Code、Codex 等），发送：**"install this"**：

```
https://github.com/userAlex110/TreeTab
```

Agent 会带你完成安装，约 1 分钟。

### 方式二：手动安装

**1. 克隆仓库**

```bash
git clone https://github.com/userAlex110/TreeTab.git
```

**2. 加载 Chrome 扩展**

1. 打开 Chrome，访问 `chrome://extensions`
2. 启用右上角的 **开发者模式**
3. 点击 **加载已解压的扩展程序**
4. 选择克隆仓库中的 `extension/` 文件夹

**3. 打开新标签页**

你将看到 TreeTab 看板。

---

## 功能特性

### 上方区域 - 标签组管理

- **显示 Chrome 原生标签组**，按横向卡片布局
- **拖拽管理** - 将下方标签页拖入分组，或在分组间移动
- **重命名分组** - 点击组名即可编辑
- **一键关闭组内所有标签页**
- **未分组标签** - 独立卡片显示，支持拖入分组

### 下方区域 - 域名分组

- **按域名自动分组** 所有标签页
- **主页特殊分组** - Gmail、X、YouTube、LinkedIn、GitHub 主页聚合
- **重复检测** - 高亮显示重复页面，一键清理
- **展开查看更多** - 每组显示前 8 个，点击 "+N more" 展开
- **拖拽到分组** - 将标签页拖入上方分组或新建分组

### 核心功能

| 功能 | 描述 |
|------|------|
| 🔍 重复检测 | 自动识别相同页面，显示 (2x) 标记，一键去重 |
| 🎯 快速跳转 | 点击任意标签直接切换到该标签页 |
| ✨ 关闭动效 | 满足感的滑过动画 + 彩纸爆发 + 音效 |
| 🏷️ 自定义标题 | 针对特定系统（如智慧教育）显示友好标题 |
| 📊 徽章计数 | 工具栏徽章显示标签数量，颜色编码健康度 |
| 🔒 100% 本地 | 数据存储在 `chrome.storage.local`，永不离开设备 |

---

## 徽章颜色含义

| 颜色 | 标签数 | Hex | 含义 |
|------|--------|-----|------|
| 🟢 绿色 | 1-10 | #5a9a7a | 状态良好，一切可控 |
| 🟡 琥珀色 | 11-20 | #b8a85e | 开始变多，注意整理 |
| 🔴 红色 | 21+ | #b35a5a | 该清理了！ |

---

## 自定义标题支持

针对特定域名自动显示更友好的标题：

| 域名 | 示例 |
|------|------|
| `ehall.cdu.edu.cn` | 智慧教育 · 流程管理 · abc12345 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 扩展框架 | Chrome Manifest V3 |
| 后台脚本 | Service Worker |
| 存储 | `chrome.storage.local` |
| 音效 | Web Audio API（合成，无音频文件） |
| 动画 | CSS 过渡 + JS 彩纸粒子 |

---

## 权限说明

```json
"permissions": [
  "tabs",        // 读取和管理标签页
  "activeTab",   // 激活标签页
  "storage",     // 本地存储
  "tabGroups"    // 管理标签组
]
```

---

## 常见问题

### 为什么看不到我的标签组？

标签组是 Chrome 的原生功能。如果你没有创建过标签组，上方区域会显示"未分组"卡片。你可以：
1. 在 Chrome 中右键标签 → "将标签页添加到新组"
2. 或者从下方拖拽标签到"新建分组"区域

### 如何退出 TreeTab？

TreeTab 替换的是新标签页。要恢复正常，只需：
1. 在地址栏输入任意网址（如 `chrome://extensions`）
2. 或者按 `Cmd/Ctrl + T` 后在地址栏输入网址

### 数据会上传吗？

不会。所有数据存储在 `chrome.storage.local`，完全本地运行。

---

## 许可证

MIT

---

## 开源致谢

本项目在 [TabOut](https://github.com/zarazhangrui/tab-out) 的基础上开发而来，感谢原作者的开源贡献。

遵循开源精神，本项目继续使用 MIT 许可证，并在代码中保留了原有的版权和许可声明。

---

**保持对标签页的掌控，从 TreeTab 开始。**
